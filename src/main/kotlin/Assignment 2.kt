//using a when or control statement expression create a simple calculator.use the concepts of input output.


fun main(args: Array<String>) {
    val a=10
    val b=20

    val c=if(a>b)a else b

    val char="admin"
    val result=when (char){
        "admin"->"User is admin"
        "admin"->"User is admin"
        "admin"->"User is admin"
        "admin"->"User is admin"
    }
}